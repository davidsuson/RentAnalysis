% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_parallel_trends_table.R
\name{build_parallel_trends_table}
\alias{build_parallel_trends_table}
\title{build_parallel_trends_table}
\usage{
build_parallel_trends_table(regressions, alpha = 0.05, pre_threshold = -1)
}
\arguments{
\item{regressions}{A named list of fitted \code{fixest} model objects. Each
name must follow the pattern \code{"<dwelling>_<bedroom>_<state>"} (e.g.
\code{"House_2_NSW"}), matching the underscore-delimited structure expected
by \code{stringr::str_split_fixed(..., "_", 3)}.}

\item{alpha}{Numeric. The significance level passed through to
\code{extract_model_details()} for flagging significant pre-treatment
coefficients. Defaults to \code{0.05}.}

\item{pre_threshold}{Numeric. The relative-time cutoff passed through to
\code{extract_model_details()} for identifying pre-treatment ("lag")
coefficients. Defaults to \code{-1}.}
}
\value{
A data frame with one row per unique dwelling/bedroom
combination and one column per state, plus leading \code{Dwelling} and
\code{Bedrooms} columns identifying each row. Each state column holds a
character string of the form \code{"N = <n_obs>:nSig = <n_sig>"}
summarising that dwelling/bedroom/state regression.
}
\description{
Given a named list of fitted \code{fixest} models with each item corresponding to a
dwelling type / bedroom count / state combination, the function builds a wide summary
table with one row per dwelling/bedroom combination and one column per
state. Each cell reports the number of observations and the number
of significant pre-treatment ("lag") coefficients for that regression.
}
