% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_parallel_trends.R
\name{test_parallel_trends}
\alias{test_parallel_trends}
\title{test_parallel_trends}
\usage{
test_parallel_trends(
  data,
  treated,
  controls,
  treatment_time = c(2021, 1),
  bin_width = 4,
  baseline_quarter = -1,
  unit
)
}
\arguments{
\item{data}{A data frame containing the panel data, including (at
least) the columns \code{State}, \code{Dwelling}, \code{Bedrooms},
\code{Year}, \code{Quarter}, and \code{New_Bonds}.}

\item{treated}{A character string giving the name of the treated state
(e.g. \code{"VIC"}).}

\item{controls}{A character vector of candidate control states. A
separate event-study regression is estimated against \code{treated}
for each control state, within each dwelling type and bedroom count
combination.}

\item{treatment_time}{A length-2 numeric vector giving the treatment
date as \code{c(year, quarter)}. Defaults to \code{c(2021, 1)}.}

\item{bin_width}{Integer giving the number of quarters to keep as
individual leads/lags around treatment before pooling the rest into
endpoint bins. \code{Relative_Time} is capped at \code{+bin_width} and
\code{-bin_width}, so periods beyond that are grouped into a single
"bin_width or more" category on each side. Defaults to \code{4}.}

\item{baseline_quarter}{The relative-time value used as the omitted reference
period in the event-study specification (passed to \code{ref} in
\code{fixest::i()}). Defaults to \code{-1}, the quarter immediately preceding
treatment.}

\item{unit}{A character string giving the unit of observation,
either \code{"State"} or \code{"LGA"}. Determines the fixed effects
(\code{State + Quarter} vs. \code{LGA + Quarter}), the clustering/vcov used
(heteroskedasticity-robust for \code{"State"}, clustered by \code{Region} for
\code{"LGA"}), and which columns are treated as controls.}
}
\value{
A named list of \code{fixest} model objects, one per
dwelling/bedroom/control combination. Each element is the fitted
event-study regression for that segment, containing the estimated
relative-time coefficients (leads and lags around \code{treatment_time})
used to assess parallel trends. List names take the form
\code{"<dwelling>_<bedroom>_<control>"}.
}
\description{
Fits an event-study difference-in-differences specification for each
combination of dwelling type, bedroom count, and candidate control
state, comparing the treated state against each control around the
specified treatment date. See report for more details.
}
