% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_parallel_trends_table.R
\name{extract_model_details}
\alias{extract_model_details}
\title{extract_model_details}
\usage{
extract_model_details(model, pre_threshold, alpha)
}
\arguments{
\item{model}{A fitted \code{fixest} model object (e.g. output of
\code{fixest::feols()}) whose coefficient names encode a relative-time
interaction term of the form \code{"...::<integer>:..."}.}

\item{pre_threshold}{Numeric. The relative-time cutoff (inclusive) below
which a coefficient is treated as a pre-treatment "lag" coefficient.
Coefficients with \code{relative_time <= pre_threshold} are checked for
significance.}

\item{alpha}{Numeric. The significance level (e.g. \code{0.05}) below which a
lag coefficient's p-value counts as significant.}
}
\value{
A list with two elements:
\describe{
\item{n_obs}{Number of observations used to fit \code{model}.}
\item{n_sig}{Number of pre-treatment lag coefficients with
\verb{Pr(>|t|)} below \code{alpha}.}
}
}
\description{
Pulls the coefficient table from a fitted \code{fixest} model, identifies the
pre-treatment ("lag") relative-time coefficients — those at or before
\code{pre_threshold} periods relative to treatment — and counts how many of
them are statistically significant at the given \code{alpha} level.
}
